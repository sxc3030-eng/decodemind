# DecodeMind + GeniA — End-of-session status

**Date:** 2026-05-19
**Session length:** ~2 days of agent-driven work
**Total commits across both repos:** 40+

## DecodeMind — `D:\decodemind\` branch `v1`

| | Status |
|---|---|
| Phase 0 spike | ✅ Done (`phase-0` branch) |
| V1 sprints 1-8 | ✅ Done |
| Severity routing | ✅ Fixed |
| ESLint config (CommonJS + globals + tests) | ✅ Fixed |
| Apply-fix verify-after-write | ✅ Done |
| ESLint fix.range exposure | ✅ Done (latest commit) |
| Tests | ✅ 246/246 |
| TypeScript strict | ✅ 0 errors |
| Husky pre-commit | ✅ Active |
| Production build | ✅ Emits SW + worker chunks |
| Tested on real code | ✅ proxy.py (mamy) — 5 fixes applied, verified on disk |
| Standalone deploy | 🔲 User action (buy `decodemind.dev`, connect Cloudflare Pages) |

## GeniA — `D:\GeniA\` branch `feature/archylearning`

| | Status |
|---|---|
| Scanner integrated (Phase A) | ✅ Done — Complete.tsx ScanPanel |
| Phase B (auto-gen + auto-scan) | ✅ Done — Anthropic API + in-memory scan |
| Secrets rotation (local) | ✅ Done — 6 secrets regenerated |
| `.env.example` cleanup | ✅ Done — 5 files |
| `.gitignore` hardening | ✅ Done |
| Deploy script (paramiko) | ✅ Done — `scripts/rotation/transfer_rotation.py` |
| Deploy script (bash) | ✅ Done — `scripts/rotation/deploy_rotation.sh` |
| Rotation runbook | ✅ Done — `docs/SECURITY_ROTATION_2026-05-19.md` |
| TypeScript + build | ✅ Clean |
| i5 production rotation | 🔲 User action (run paramiko script with SSH password) |
| Third-party key rotation | 🔲 User action (Anthropic / Groq / HF / PayPal / Spotify / Gmail dashboards) |
| Git history scrub (BFG) | 🔲 Optional user action (destructive force-push) |
| PR `feature/archylearning` → `main` | 🔲 User action (`gh pr create` command in PR_BODY_archylearning.md) |

## User's manual to-do list (in priority order)

1. **URGENT — push secrets to i5:**
   ```sh
   cd D:\GeniA
   pip install paramiko
   python scripts/rotation/transfer_rotation.py <i5-host> --dry-run  # preview
   python scripts/rotation/transfer_rotation.py <i5-host>             # actual push
   ```

2. **URGENT — rotate third-party API keys** on their dashboards:
   - https://console.anthropic.com/settings/keys
   - https://console.groq.com/keys
   - https://huggingface.co/settings/tokens
   - https://developer.paypal.com/dashboard (sandbox, low priority)
   - https://developer.spotify.com/dashboard
   - https://myaccount.google.com/apppasswords (the Gmail app password)

3. **Test the scanner integration in browser:**
   ```sh
   cd D:\GeniA\apps\web
   npm run dev
   # Then visit http://localhost:3000/builder, complete a brief, click "Scan generated code"
   ```

4. **Create the GitHub PRs** (commands ready in PR body docs):
   - DecodeMind: see `D:\decodemind\docs\PR_BODY_v1.md` for the `gh pr create` invocation
   - GeniA: see `D:\GeniA\docs\PR_BODY_archylearning.md`

5. **Re-test DecodeMind standalone on NetGuardPro full scan** — last session showed 199 findings in cleanguard.py with 4 critical (3× S202 tarfile.extractall = real Zip Slip + 1× F821 undefined name).

6. **When happy: buy `decodemind.dev` (~$15/yr)** and connect Cloudflare Pages. Domain availability was confirmed in Phase 0 research.

7. **Optional: scrub git history** with BFG to purge OLD secret values from `git log -S` searches. Runbook step 4 in `D:\GeniA\docs\SECURITY_ROTATION_2026-05-19.md`.

## What's intentionally NOT done (V2 or out of scope)

DecodeMind V2 backlog (per spec section 6):
- L3 LLM-generated fixes
- "Second opinion" via user-provided API key
- Inline Monaco patch editor
- More languages (Go, Rust, Java, C#, PHP)
- VS Code / JetBrains extensions
- GitHub Action
- Fine-tuned custom model
- Team mode with shared rule library
- Full SARIF 2.1.0 conformance
- Cross-file refactor
- Interprocedural taint/dataflow analysis

GeniA scanner extension Phase C ideas:
- Port the ast-grep Vite plugin so GeniA also gets the custom rules (not just Ruff/ESLint/Prettier)
- Per-user JWT for mobile media auth (replace the static EXPO_PUBLIC_MEDIA_TOKEN)
- Rate-limiting on `/api/builder/generate-and-scan` to prevent Anthropic cost abuse
- Move the scanner libs to a `packages/scanner` workspace package so other apps can consume them

## Future session — quick-start

If a future session needs to resume:

1. Read `MEMORY.md` (index) and `project_decodemind.md` for context
2. Check git state: `cd D:\decodemind && git log --oneline -5` and same for GeniA
3. Verify nothing's broken: `npm run typecheck && npm test` in both repos
4. Pick a TODO from "User's manual to-do list" above

State of the world is captured in this doc — read it first.
