id: bash-sudo-no-password
language: Bash
category: security
severity: warning
message: sudo -n runs as root non-interactively — usually means a passwordless
  sudoers entry that broadens the attack surface
rule:
  any:
    - pattern: sudo -n $CMD
    - pattern: sudo -n $$$ARGS
