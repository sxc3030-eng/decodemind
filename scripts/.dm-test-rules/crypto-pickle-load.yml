id: crypto-pickle-load
language: Python
category: security
severity: error
message: pickle.loads() deserializes arbitrary objects — remote code execution
  if data is untrusted
rule:
  pattern: pickle.loads($X)
