id: kotlin-shared-prefs-plaintext-password
language: Kotlin
category: security
severity: error
message: Storing a password in SharedPreferences in plaintext — use EncryptedSharedPreferences or Android Keystore
rule:
  pattern: $PREFS.edit().putString("password", $V)
