id: go-exec-command-user-input
language: Go
category: security
severity: error
message: exec.Command with a variable command name — command injection if $CMD
  is user-controlled; hardcode the binary path
rule:
  pattern: exec.Command($CMD, $$$ARGS)
constraints:
  CMD:
    kind: identifier
