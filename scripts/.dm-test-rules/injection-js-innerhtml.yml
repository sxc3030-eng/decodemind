id: injection-js-innerhtml
language: JavaScript
category: security
severity: error
message: innerHTML assignment with variable — XSS risk if value is user-controlled
rule:
  pattern: $EL.innerHTML = $USER_INPUT
