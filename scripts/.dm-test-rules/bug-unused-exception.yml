id: bug-unused-exception
language: Python
category: bug
severity: warning
message: Caught exception is never referenced — silent failure risk
rule:
  pattern: |
    except Exception:
      pass
# TODO: also catch bare `except: pass` and `except SomeError: pass` variants.
