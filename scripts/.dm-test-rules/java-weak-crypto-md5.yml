id: java-weak-crypto-md5
language: Java
category: security
severity: warning
message: MessageDigest.getInstance("MD5") is cryptographically broken — use SHA-256 or stronger
rule:
  pattern: MessageDigest.getInstance("MD5")
