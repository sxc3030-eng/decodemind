id: php-empty-catch
language: PHP
category: bug
severity: warning
message: Empty catch block silently swallows exceptions — log or rethrow to keep
  failures observable
rule:
  pattern: |
    catch ($T $E) {
    }
