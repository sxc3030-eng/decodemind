id: go-http-redirect-user-input
language: Go
category: security
severity: warning
message: http.Redirect with a variable URL — open-redirect risk if $V is request-derived; validate the destination against an allowlist
rule:
  pattern: http.Redirect($W, $R, $V, $CODE)
  constraints:
    V:
      kind: identifier
