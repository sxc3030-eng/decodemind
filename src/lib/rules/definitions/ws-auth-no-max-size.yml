id: ws-auth-no-max-size
language: Python
category: security
severity: warning
message: WebSocket without max_size — possible memory exhaustion attack
rule:
  pattern: websockets.serve($HANDLER, $$$ARGS)
