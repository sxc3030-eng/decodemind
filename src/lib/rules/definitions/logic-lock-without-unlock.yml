id: logic-lock-without-unlock
language: Python
category: logic
severity: error
message: Lock acquired without try/finally — deadlock risk on exception
rule:
  any:
    - pattern: lock.acquire()
    - pattern: mutex.acquire()
    - pattern: self.lock.acquire()
    - pattern: self._lock.acquire()
    - pattern: self.mutex.acquire()
