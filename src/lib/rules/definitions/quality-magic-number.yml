id: quality-magic-number
language: Python
category: quality
severity: info
message: Magic number literal — extract to a named constant
rule:
  all:
    - kind: assignment
    - pattern: $VAR = $NUM
constraints:
  NUM:
    regex: '^-?[0-9]+(\.[0-9]+)?$'
