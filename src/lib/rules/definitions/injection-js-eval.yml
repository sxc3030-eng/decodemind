id: injection-js-eval
language: JavaScript
category: security
severity: error
message: eval() executes arbitrary JavaScript code — code injection if input is user-controlled
rule:
  pattern: eval($X)
