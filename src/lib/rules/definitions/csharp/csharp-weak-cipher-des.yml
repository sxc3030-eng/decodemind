id: csharp-weak-cipher-des
language: C#
category: security
severity: error
message: DES uses a 56-bit key and is trivially brute-forceable — use AES-GCM or AES-CBC with HMAC instead
rule:
  pattern: DES.Create()
