id: crypto-jwt-none-alg
language: JavaScript
category: security
severity: error
message: JWT verified with 'none' algorithm — this disables signature verification entirely
rule:
  pattern: "jwt.verify($T, $S, { algorithms: ['none'] })"
