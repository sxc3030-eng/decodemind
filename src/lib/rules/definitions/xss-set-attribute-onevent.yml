id: xss-set-attribute-onevent
language: JavaScript
category: security
severity: error
message: setAttribute with an event handler name — XSS risk if the value is user-controlled
rule:
  pattern: $EL.setAttribute($ATTR, $X)
  constraints:
    ATTR:
      regex: '^"on\w+'
