id: bug-react-key-missing
language: TypeScript
category: bug
severity: warning
message: React array element without key prop — re-render bugs
rule:
  pattern: $ARR.map(($ITEM) => <$TAG $$$PROPS>$$$CHILDREN</$TAG>)
# TODO: also catch self-closing tags and arrow body without parens.
# Cannot check absence of key prop in $$$PROPS without negative matching.
