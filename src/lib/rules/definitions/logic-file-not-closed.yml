id: logic-file-not-closed
language: Python
category: logic
severity: warning
message: open() without with-statement may leak file descriptors
rule:
  pattern: $VAR = open($$$)
# TODO: this also flags `with open(...) as f` if assigned outside with — refine later.
