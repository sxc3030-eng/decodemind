id: bug-dict-get-no-default
language: Python
category: bug
severity: info
message: dict.get() without default returns None — caller may not handle it (V3 backlog — hangs WASM)
rule:
  pattern: __decodemind_v3_backlog_noop__
# Valid Python identifier (no special chars) that won't appear in real code.
# ast-grep parses it as a name expression — no AST ambiguity, no WASM hang.
