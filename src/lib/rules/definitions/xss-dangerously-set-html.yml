id: xss-dangerously-set-html
language: JavaScript
category: security
severity: error
message: dangerouslySetInnerHTML with variable — XSS risk if value is user-controlled, React bypasses escaping
rule:
  pattern: "dangerouslySetInnerHTML={{ __html: $USER }}"
